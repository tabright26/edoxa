apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-gateway.fullname" . }}-test-connection"
  labels:
{{ include "web-gateway.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "web-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
