apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-status.fullname" . }}-test-connection"
  labels:
{{ include "web-status.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "web-status.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
