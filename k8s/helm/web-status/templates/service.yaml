apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-status.fullname" . }}
  labels:
{{ include "web-status.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "web-status.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
