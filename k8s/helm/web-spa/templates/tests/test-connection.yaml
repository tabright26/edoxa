apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-spa.fullname" . }}-test-connection"
  labels:
{{ include "web-spa.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "web-spa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
