apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payment-api.fullname" . }}-test-connection"
  labels:
{{ include "payment-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "payment-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
