apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cashier-api.fullname" . }}-test-connection"
  labels:
{{ include "cashier-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cashier-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
