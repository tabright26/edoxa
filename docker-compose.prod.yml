version: '3.7'

services:
  mssql:
    image: mcr.microsoft.com/mssql/server:2017-CU14-ubuntu

  redis:
    image: redis:5.0.5

  rabbitmq:
    image: rabbitmq:3-management-alpine

  seq:
    image: datalust/seq:latest

  identity.api:
    image: ${ACR_REGISTRY:-edoxa}/prod/identity.api:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Identity.Api/Dockerfile

  cashier.api:
    image: ${ACR_REGISTRY:-edoxa}/prod/cashier.api:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Cashier.Api/Dockerfile

  payment.api:
    image: ${ACR_REGISTRY:-edoxa}/prod/payment.api:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Payment.Api/Dockerfile

  notifications.api:
    image: ${ACR_REGISTRY:-edoxa}/prod/notifications.api:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Notifications.Api/Dockerfile

  games.api:
    image: ${ACR_REGISTRY:-edoxa}/prod/games.api:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Games.Api/Dockerfile

  clans.api:
    image: ${ACR_REGISTRY:-edoxa}/prod/clans.api:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Clans.Api/Dockerfile

  challenges.api:
    image: ${ACR_REGISTRY:-edoxa}/prod/challenges.api:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Challenges.Api/Dockerfile

  challenges.web.jobs:
    image: ${ACR_REGISTRY:-edoxa}/prod/challenges.web.jobs:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Challenges.Web.Jobs/Dockerfile

  challenges.web.aggregator:
    image: ${ACR_REGISTRY:-edoxa}/prod/challenges.web.aggregator:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Challenges.Web.Aggregator/Dockerfile

  challenges.web.gateway:
    image: ${ACR_REGISTRY:-edoxa}/prod/challenges.web.gateway:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Gateway/Dockerfile

  web.spa:
    image: ${ACR_REGISTRY:-edoxa}/prod/web.spa:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Web.Spa/Dockerfile-prod

  web.status:
    image: ${ACR_REGISTRY:-edoxa}/prod/web.status:${ACR_TAG:-latest}
    build:
      context: .
      dockerfile: src/eDoxa.Web.Status/Dockerfile