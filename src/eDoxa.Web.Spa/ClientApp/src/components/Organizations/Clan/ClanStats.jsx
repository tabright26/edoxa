import React from "react";
import { Card, CardBody, CardHeader } from "reactstrap";

const ClanStats = ({ stats }) => {
  return (
    <Card className="card-accent-primary">
      <CardHeader>Stats</CardHeader>
      <CardBody></CardBody>
    </Card>
  );
};

export default ClanStats;
